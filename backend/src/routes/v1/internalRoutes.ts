import { Router } from 'express';

const router = Router();

/**
 * @summary Internal (authenticated) routes configuration
 * @description Routes requiring authentication
 */

/**
 * @remarks
 * Internal routes will be added here as features are implemented
 * Example structure:
 * - /internal/menu (admin operations)
 * - /internal/establishment-info (admin operations)
 * - /internal/gallery (admin operations)
 * - /internal/promotions (admin operations)
 */

export default router;
